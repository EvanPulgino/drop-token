import * as Constants from './constants';

export function makeBackgroundEmpty(className) {
    return className + " " + Constants.BACKGROUND_EMPTY;
}

export function makeBackgroundRed(className) {
    return className + " " + Constants.BACKGROUND_RED;
}

export function makeBackgroundYellow(className) {
    return className + " " + Constants.BACKGROUND_YELLOW;
}

export function makeClickable(className) {
    return className + " " + Constants.CLICKABLE;
}

export function makeHidden(className) {
    return className + " " + Constants.HIDDEN;
}

export function makeSelected(className) {
    return className + " " + Constants.SELECTED;
}